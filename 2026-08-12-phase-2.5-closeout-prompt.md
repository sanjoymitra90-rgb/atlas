# ATLAS — Phase 2.5 close-out

**Paste this whole file as your opening instruction.**

**For:** the AI coding agent closing out Phase 2.5.
**From:** verification pass against the Phase 2.5 result, 2026-08-12, run at commit `71ec13a`.
**Status:** CI is red. The live site is stranded at `ee7f62f` — the end of Phase 2. None of
Phase 2.5 is deployed.

**No line numbers anywhere in this document, or in anything you write.** Anchor on function names,
file paths, element IDs and `data-testid` values.

---

## 0. Read these first, in this order

1. `claude_memory.md` — project state, decisions already made, and the three ways this project
   fails. §6 is the important part.
2. `2026-08-12-phase-2.5-spec.md` — the phase you are closing. Most of it landed; this document
   covers what did not.
3. `docs/SPEC.md` — the system as it is.

Do not read the Phase 1 or Phase 2 specs unless something here points you at them.

---

## 1. Where things actually stand

Phase 2.5 delivered eleven commits. **The behaviour fixes are correct and verified** — Tasks G and
H were checked by executing the modules directly, not by reading them. Do not re-do them, and do
not "improve" them.

What did not land:

- **All four new tests in Task I fail.** They were written against a UI that does not exist.
- **The screenshot capture script was repaired but never run.**
- **Three claims in the documentation are false.**

CI caught the test failures and correctly refused to deploy. That is the pipeline working as
designed. Your job is to make it green.

**Do not start Task D.** It was formally deferred and the deferral is recorded in
`claude_memory.md`. Do not start anything from Phases 3–6.

---

## 2. Hard constraints

1. **Commit as you go.** One commit per numbered item below. A single end-of-phase commit is a
   failure condition.
2. **Every test you touch must be proven able to fail.** Delete or revert the fix it covers, run
   it, confirm it goes red, restore the fix. **State in the commit message that you did this.**
   This is not optional — see §6.
3. **Do not change application behaviour to make a test pass.** Every fix these tests cover is
   already correct and verified. If a test disagrees with the application, the test is wrong.
4. **If a document and the code disagree, fix the document in the same commit as the code.**
5. **If any statement in this document turns out to be wrong when you check it against the code,
   say so and stop** rather than working around it.

---

## 3. Task J — fix the four broken tests

All four are in `e2e/gap/gap-review.spec.cjs`. They share one root cause and three of them are in
the wrong module.

### The root cause

The gap export modal (`#gap-export-modal`) contains **three** buttons, wired to
`confirmGapExport('filtered')`, `confirmGapExport('all')` and `confirmGapExport('pairs')`. Their
visible labels are "Filtered Results", "All Results" and "Pair Summary".

**There is no button whose text contains "CSV" or "JSON".** The format is always CSV; the choice is
about scope. `confirmGapExport()` sets `window.gapExportAllData` / `window.gapExportPairs`, then
calls `exportGapData()` on a short timeout.

Three of the four tests look for buttons that do not exist. Because every interaction is wrapped in
`if (await x.isVisible({ timeout: 2000 }).catch(() => false))`, the missing element is swallowed
and the test continues, failing much later at a download timeout or a count assertion instead of at
the point where the assumption broke.

### J0. Remove every `if (await …isVisible().catch(() => false))` guard — do this first

Across all four tests. If an element is required for the test to mean anything, its absence must
fail the test immediately and loudly. These guards are the reason four broken tests looked
plausible enough to commit.

### J1. There is already a working export pattern in this repo — use it

`e2e/gap/gap-dup.spec.cjs`, the test named *"export CSV includes duplicate in pairing summary"*,
passes today and exports without touching the UI:

```js
const download = page.waitForEvent('download');
await page.evaluate(() => {
  window.gapExportAllData = true;
  window.exportGapData();
});
const dl = await download;
const content = require('fs').readFileSync(await dl.path(), 'utf8');
```

Copy this shape for B3, B5 and B6. Driving the export through the window bridge is more robust than
clicking through a modal, and it is the established convention in this suite.

### J2. B3 — CSV header with a comma in a custom column name

Currently fails on a download timeout, because it clicks a "CSV" button that does not exist.

**Two problems, fix both:**

- **It never exports.** Replace the modal-clicking with the J1 pattern.
- **Its assertion cannot fail.** `expect(commaCount).toBeGreaterThanOrEqual(9)` passes on the
  default header, which has twelve columns and eleven commas, whether or not a custom column was
  ever added. Assert the **exact** expected column count with the comma-named column present.

For the custom column itself, the reliable route is to set `gapAdditionalColumns` directly inside
`page.evaluate` before exporting. The UI route, if you prefer it: the `+ Add Column` button calls
`addGapAdditionalColumn()`, rows render into `#gap-additional-columns`, and the mapping is applied
by `confirmGapColumnMapping()` (testid `gap-analyze-btn`).

**Prove it fails:** revert `csvCell` on the header row in `exportGapData()` — the header becomes
`allHeaders.join(',')` — and confirm the test goes red.

### J3. B5 — blob URL revocation

Currently fails at `expect(counts.create).toBeGreaterThan(0)`, because it clicks a "JSON" button in
the gap analyzer. **The gap analyzer has no JSON export.** JSON export is `exportSessionJSON()`,
which belongs to the optimizer and uses a `data:` URI, not a blob — so it would not have exercised
this fix even if it had been reachable.

**Fix:** use the gap CSV export via the J1 pattern. All three blob export sites go through the
single `downloadBlob()` helper in `src/core/download.js`, so exercising one path genuinely covers
the fix.

Keep the existing stub-and-count approach — it is sound. Assert both that `create > 0` and that
`revoke >= create`.

If you want coverage of all three blob sites, add `exportShareReport()` in the optimizer spec as a
second test. Optional.

**Prove it fails:** remove the `URL.revokeObjectURL(url)` line from `downloadBlob()` and confirm
the test goes red.

### J4. B6 — Gap column absent from the export summary

Same root cause as B3: no "CSV" button, download times out. Same fix — use the J1 pattern.

The assertions themselves are fine and should be kept.

**Prove it fails:** add a `Gap` column back into the summary header in `exportGapData()` and
confirm the test goes red.

### J5. B2 — scenario name XSS — move this to the optimizer

This test is in the wrong module twice over. It writes to `localStorage['gapScenarios']`; the app
uses **`atlas-opt-scenarios`**. It then opens `#gap-column-modal`, the gap analyzer's settings
modal, looking for a scenario list that is rendered by `renderScenarioList()` into `#scenario-list`
inside `#scenarios-modal` (testid `scenarios-modal`), opened by `openScenariosModal()` — all of
which belong to the **optimizer**, not the gap analyzer.

**Fix:**

- Move the test to `e2e/optimizer/opt-review.spec.cjs`, where the module it tests actually lives.
  Delete it from `gap-review.spec.cjs`.
- Seed `atlas-opt-scenarios` with a scenario carrying the payload as its `name`. Read
  `getOptScenarios()` and `saveOptScenario()` to get the stored object shape right —
  `renderScenarioList()` reads `name`, `ts` and `summary`.
- Open the modal via `openScenariosModal()`.
- Assert `#scenario-list` contains no element node matching the payload, **and** that its
  `textContent` includes the literal payload string.

**Prove it fails:** remove the `escapeHtml()` wrapper from `s.name` in `renderScenarioList()` and
confirm the test goes red.

---

## 4. Task K — finish what Phase 2.5 started

### K1. Run the screenshot capture script

`screenshots-before/capture-screenshots.cjs` was correctly rewritten to capture eleven distinct
screens, with scrolling and modal open/close between captures. **It was never executed.** The
folder still holds the previous run's output, under the old names, including three byte-identical
files.

- Run it.
- **Hash the eleven outputs and confirm all eleven hashes are distinct.** This is the acceptance
  test. Do not eyeball the images.
- Delete the stale files left over from the previous run.
- If any two hashes still match, the script is still wrong for those screens — fix it and re-run.

The folder is gitignored and untracked, correctly. Keep it that way.

### K2. Decide H3 — the country code tooltip

`validateUKNumber()` now returns the reason `"Non-UK destination"` with no country code. That part
is correct and was Sanjoy's decision.

But `ukPillHtml()` renders `title="' + escapeHtml(row.ukValidationReason) + '"`, so **the tooltip
contains no country code either.** The country code is not surfaced anywhere in the application.
Both the Phase 2.5 changelog entry and the H1/H3 commit message state that the full prefix is
"preserved in tooltip". That is false.

**Pick one and do it:**

- **Put the prefix back in the tooltip.** Keep the chip label as `"Non-UK destination"` and give
  `ukPillHtml()` the full cleaned prefix separately — for example by returning a
  `countryCode` field from `validateUKNumber()` and carrying it on the row. This is the better
  option: Phase 3 redesigns these chips and having the data available is worth more than not.
- **Or correct the two claims** to say the country code was dropped.

Whichever you choose, `gapReasonBucket()` must keep matching `non-uk`, and the existing cases in
`src/auditor/validate.test.js` must be updated in the same commit.

### K3. Correct the remaining documentation errors

In `docs/CHANGELOG.md`:

- **Phase 2 section, still wrong:** *"XSS: customer names — `s.name` escaped in all tooltip and
  popup templates."* B2 escaped a **scenario** name, in `renderScenarioList()`, in one place. It is
  not a customer name and it is not "all templates". This was on the original list of five errors
  and was missed.
- **Phase 2 section, newly introduced:** *"Chart.js 4.4.1, html2pdf.js 0.10.1, DHTMLX Gantt 8.0"*.
  `package.json` declares `^4.5.1`, `^0.14.0` and `^10.0.1`. Three of the four versions are wrong.
  Take the numbers from `package.json`, not from memory.
- **Phase 2 section:** *"76 unit tests"*. There are 77 `it()` blocks across the five remaining unit
  test files. Recount after your test changes land and state the real number, or drop the count.
- **Phase 2.5 section:** the H3 tooltip claim, per K2. And *"Screenshot baseline fixed"* overstates
  what happened — the script was repaired but not run. Correct it, or make it true via K1 and leave
  it.

In `docs/SPEC.md`, invariant 6 reads that `computeCoverage()` and `computeGreenPlan()` *"read the
module-level `regions` binding"*. They no longer do — `regions` and `getRegionCost` are passed in
as part of `input`. The real remaining impurities are that `computeCoverage()` **mutates its input**
(it writes `idx` onto the caller's customer objects) and that it calls module-scope
`findNearestRegionIdx()`. Reword to describe those two, and keep the invariant as "not pure" —
because it is not, and Task D is deferred.

---

## 5. Task L — ship it

1. Run `npm test` locally. Unit tests, then build, then e2e. Everything green before you push.
2. Push to `main`.
3. Watch the "Build and deploy" workflow in the Actions tab. It must go green.
4. Open the live site at **https://sanjoymitra90-rgb.github.io/qwen-test-atlas/** and confirm it
   loads **and responds** — upload `fixtures/gap-core.csv` and check the dashboard populates. A
   page that renders but does nothing means the deploy did not take.
5. Confirm the live app is serving Phase 2.5 and not `ee7f62f`.

**Report the run URL and the result.** If it goes red again, read the failing step, report what
failed, and stop — do not push a second speculative fix on top.

---

## 6. Why constraint 2 exists — read this before you write a test

This project has shipped tests that report coverage they do not have in **three consecutive
phases**:

- **Before Phase 1** — six Playwright tests re-implemented application logic inside
  `page.evaluate()` and asserted against their own copy. The physics test computed the
  light-in-fibre floor and then discarded it. The suite was reported as "182 passing".
- **Phase 2** — `src/auditor/pairing.test.js` asserted `expect(40 + 60).toBe(100)` and checked a
  property on an object it had constructed two lines earlier. It imported nothing from the
  application and would have passed if `pairGapCalls()` were deleted.
- **Phase 2.5** — four tests written against a UI that does not exist, three of them aimed at the
  wrong module entirely.

Each time, the tests looked plausible and were committed with a message describing coverage that
was not there. Each time, the next person had to discover it.

**One check catches all three, and it takes seconds: delete the thing the test names, run the test,
and confirm it goes red.** None of these would have survived it.

Do this for every test you touch, and say so in the commit message.

---

## 7. Definition of done

- [ ] All four Task I tests pass, and each has been proven to fail with its fix reverted
- [ ] B2 lives in `e2e/optimizer/opt-review.spec.cjs` and is gone from `gap-review.spec.cjs`
- [ ] No `if (await …isVisible().catch(() => false))` guards remain in `gap-review.spec.cjs`
- [ ] B3 asserts an exact column count, not a lower bound
- [ ] The capture script has been run; eleven output files, eleven distinct hashes; stale files
      deleted
- [ ] H3 decided and implemented; the changelog and the commit history agree with the code
- [ ] The three remaining changelog errors corrected; the unit test count is real
- [ ] `SPEC.md` invariant 6 describes the actual impurities
- [ ] `npm test` green locally
- [ ] The "Build and deploy" workflow green, and the live site verified serving Phase 2.5
- [ ] Multiple commits, not one

---

## 8. One item that is not yours

**Settings → Pages → Build and deployment** should show **Source: GitHub Actions**. Evidence
suggests it already does — the legacy `pages-build-deployment` workflow stopped firing once the new
workflow was added — but nobody has read the setting directly. Sanjoy will confirm this. Do not
change repository settings yourself.

---

## 9. When you are done

Update `claude_memory.md`:

- Move Phase 2.5 from open to complete, with the close-out folded in
- Collapse §5 ("What the Phase 2 audit found") to a short paragraph — it has served its purpose
- Record whichever H3 option was chosen, in §3, so it is not reopened
- Add the fourth failure-mode instance to §6 if you think it warrants it, or note that CI caught
  this one, which is new and worth recording

Then stop and hand back. **Phase 3 is a separate conversation.**
